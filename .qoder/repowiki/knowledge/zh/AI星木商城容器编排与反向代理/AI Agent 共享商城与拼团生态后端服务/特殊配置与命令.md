运行需准备 PostgreSQL、RabbitMQ、Redis、MinIO 四个外部服务，并将连接串写入 `.env`（参考 `.env.example`）；开发环境启动 `uvicorn app.main:app --reload`，生产用 Dockerfile 镜像化部署；数据库迁移通过 `alembic revision --autogenerate` 生成版本后 `alembic upgrade head` 执行。